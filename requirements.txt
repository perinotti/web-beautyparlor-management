# ==============================================================================
# requirements.txt — Web Beauty Parlor Management
# ==============================================================================
# ATENÇÃO: Use sempre versões fixas (==) para garantir que todos os membros
# da equipe e o servidor de produção rodem exatamente o mesmo ambiente.
#
# Para atualizar uma dependência:
# 1. Teste localmente com a nova versão
# 2. Atualize o número aqui
# 3. Commit com mensagem: "chore: atualiza <pacote> de X.Y.Z para A.B.C"
# ==============================================================================

# --- Core Framework ---
fastapi==0.116.1
uvicorn==0.35.0

# --- Banco de Dados ---
SQLAlchemy==2.0.42
alembic==1.13.3
psycopg2-binary==2.9.10
greenlet==3.2.3

# --- Segurança ---
# ATENÇÃO: bcrypt >= 4.2.0 quebra o passlib 1.7.4 (API interna mudou).
# NÃO atualizar bcrypt sem antes atualizar e testar o passlib.
bcrypt==4.0.1
passlib==1.7.4
itsdangerous==2.2.0
cryptography==45.0.3
python-jose==3.5.0
pyasn1==0.6.1
rsa==4.9.1
ecdsa==0.19.1
six==1.17.0

# --- Templates e Formulários ---
# ATENÇÃO: starlette >= 1.0.0 quebra o TemplateResponse do Jinja2.
# NÃO atualizar starlette para 1.0.0+ sem verificar compatibilidade com FastAPI.
starlette==0.47.2
Jinja2==3.1.6
MarkupSafe==3.0.2
python-multipart==0.0.20

# --- Variáveis de Ambiente ---
python-dotenv==1.1.1

# --- Validação e Tipagem ---
pydantic==2.11.7
pydantic-extra-types==2.10.5
pydantic-settings==2.10.1
pydantic_core==2.33.2
annotated-types==0.7.0
typing_extensions==4.14.1
typing-inspection==0.4.1
email_validator==2.2.0
dnspython==2.7.0

# --- Timezone ---
# TODO: remover no Commit 8 — será substituído por zoneinfo (nativo Python 3.9+)
pytz==2025.2

# --- HTTP e Rede ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.9.0
sniffio==1.3.1
certifi==2025.4.26
idna==3.10
urllib3==2.5.0

# --- Servidor e Performance ---
httptools==0.6.4
watchfiles==1.1.0
websockets==15.0.1
# uvloop acelera o servidor no Linux/macOS (não disponível no Windows)
uvloop==0.21.0; sys_platform != "win32"

# --- Migrations e CLI ---
Mako==1.3.11
click==8.2.1
typer==0.16.0
shellingham==1.5.4
rich==14.0.0
rich-toolkit==0.14.9
Pygments==2.19.2
markdown-it-py==3.0.0
mdurl==0.1.2

# --- Serialização ---
orjson==3.11.1
ujson==5.10.0
PyYAML==6.0.2

# --- Outros ---
cffi==1.17.1
pycparser==2.22